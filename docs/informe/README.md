# Informe de estado para el equipo

`estado-y-plan.tex` es la fuente. Para regenerar el PDF:

```
cd docs/informe && pdflatex -interaction=nonstopmode estado-y-plan.tex && pdflatex -interaction=nonstopmode estado-y-plan.tex
```
(dos pasadas: la segunda cuadra el índice). El PDF no se versiona; copia el resultado a `dist/` o compártelo directamente.

Se escribió el 19/09 a las 01:40 y refleja el estado de ese momento: cifras en `docs/agentes/` y plan en `docs/PLAN-SABADO.md`.
