# entorno
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.python-version.local

# secretos: sólo .env.example va al repo
.env
.env.*
!.env.example

# artefactos generados (BD, caché LLM, outcomes): se regeneran con make
dist/
*.db
*.db-wal
*.db-shm

# clon local del repo de entrega (HS-Maisa-Entrega, en ../) — nunca dentro de este repo
HS-Maisa-Entrega/
la-caja-outcomes/

# personal
.claude/settings.local.json
CLAUDE.local.md
.DS_Store
*.log

# marcador de rol (merge/contratos) para los hooks: personal, nunca al repo
.claude/dueno.local
!docs/entregas.log
